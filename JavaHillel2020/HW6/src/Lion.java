public class Lion extends WildAnimal {
    public Lion(int id, int age, int weight, String color, String kind, String myVoice, boolean isPredator) {
        super (id, age, weight, color, kind, myVoice, isPredator);
    }
}
